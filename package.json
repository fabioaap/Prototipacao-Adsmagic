{
  "name": "prototipacao-adsmagic",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently --kill-others-on-fail -n WORKSPACE,DOCS -c blue,cyan \"npm run dev:workspace\" \"npm run docs:dev\"",
    "dev:workspace": "vite",
    "dev:fo": "npm run dev --prefix Plataforma",
    "predev:all": "node -e \"console.log('dev:all usa a primeira porta livre entre 3000 e 3006')\"",
    "dev:all": "concurrently --kill-others-on-fail -n ROOT,PLATAFORMA -c blue,green \"vite\" \"npm run dev --prefix Plataforma\"",
    "docs:install": "npm install --prefix docs",
    "docs:dev": "npm run start --prefix docs -- --port 3001 --host 0.0.0.0",
    "docs:build": "npm run build --prefix docs",
    "docs:serve": "npm run serve --prefix docs",
    "docs:clear": "npm run clear --prefix docs",
    "dev:with-docs": "npm run dev",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "vue": "^3.5.22",
    "vue-router": "^4.6.3"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.4",
    "concurrently": "^9.2.1",
    "playwright": "^1.58.2",
    "vite": "^7.3.1"
  }
}
