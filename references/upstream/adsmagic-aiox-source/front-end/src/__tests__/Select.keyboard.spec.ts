import { describe, it } from 'vitest'

describe('Select keyboard navigation', () => {
    it.skip('navigates with ArrowUp/ArrowDown and confirms with Enter', () => {
        // Keys: ArrowUp, ArrowDown, Home, End, Enter, Escape
    })
})
